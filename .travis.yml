language: scala

scala:
  - 2.11.8

jdk:
  - oraclejdk8

script:
  - travis_retry sbt ++$TRAVIS_SCALA_VERSION coverage test it:test scalastyle coverageReport && sbt ++$TRAVIS_SCALA_VERSION coverageAggregate

after_success:
  - bash <(curl -s https://codecov.io/bash)
